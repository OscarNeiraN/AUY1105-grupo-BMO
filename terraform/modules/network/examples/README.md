# Network Module Examples

Esta carpeta contiene ejemplos de uso del modulo `network`.

## Ejemplo disponible

- `basic`: crea la red base con una VPC, una subnet, security group, route table, VPC Flow Logs, CloudWatch Logs y KMS.

Para ejecutar:

```bash
cd terraform/modules/network/examples/basic
terraform init
terraform plan
```
